<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_nav_graph"
    app:startDestination="@id/productsFragment">

    <fragment
        android:id="@+id/productsFragment"
        android:name="cd.ghost.catalog.presentation.productlist.ProductsFrag"
        android:label="ProductsFragment"
        tools:layout="@layout/frag_products">
        <action
            android:id="@+id/action_productsFragment_to_filterFrag"
            app:destination="@id/filterFrag"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:popEnterAnim="@anim/slide_in_left"
            app:popExitAnim="@anim/slide_out_right" />

    </fragment>

    <fragment
        android:id="@+id/filterFrag"
        android:name="cd.ghost.catalog.presentation.filter.FilterFrag"
        android:label="FilterFrag"
        tools:layout="@layout/frag_filter" />

</navigation>